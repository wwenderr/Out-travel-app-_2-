# Реализация этапов разработки

## Статус по этапам

### Этап 1 (2–4 недели) — **реализован**

Сделано:
- [x] Базовые сущности: страны, города, поездки.
- [x] Экран статистики (`StatsScreen`).
- [x] Карта посещённых городов (`VisitedMapScreen`, с `MapKit` при доступности).
- [x] Каталог городов (`CityCatalogScreen`) + поиск.
- [x] UI-добавление поездки (`AddTripSheet`) с валидацией на уровне store.

Техническая реализация:
- `OutTravelStore` и `StatsCalculator` дают данные для экранов.
- `StageOneViewModel` связывает хранилище и UI.
- `StageOneRootView` собирает основные вкладки приложения.

### Этап 2 (2–3 недели) — **частично**

Сделано:
- [x] Базовая модель `Place` и API добавления места (`addPlace`).
- [x] Базовый UI для добавления мест (`AddPlaceSheet`).
- [x] Экран гайдов (`GuidesScreen`) со списком мест, сгруппированных по городам.

Не сделано:
- [ ] Карта мест внутри конкретного города.
- [ ] Расширенные фильтры и сортировки по категориям/рейтингу в UI.
- [ ] Полноценная работа с фото (picker, хранение, предпросмотр).

### Этап 3 (1–2 недели) — **не начат**

- [ ] CloudKit sync.
- [ ] Экспорт/импорт.
- [ ] Улучшенная аналитика.
